[package]
name = "omachords-engine"
version = "0.1.0"
edition = "2024"

[dependencies]
cpal = "0.18.2"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[profile.release]
lto = "thin"
strip = true
